{
    "manager_url": {
        "title": "管理器地址",
        "type": "string",
        "description": "ncqq-manager 的后台地址，例如 http://localhost:8080"
    },
    "api_key": {
        "title": "API Key",
        "type": "string",
        "description": "管理器的 API Key"
    },
    "default_manager": {
        "title": "默认管理器 ID",
        "type": "string",
        "description": "多 ncqq-manager 面板配置时的默认面板 ID。未配置多面板时，旧 manager_url/api_key 会作为该 ID 对应的面板。",
        "default": "default"
    },
    "manager_profiles": {
        "title": "多管理器配置 JSON",
        "type": "string",
        "description": "可选。JSON 数组，每项包含 id、name、manager_url、api_key。用于同时管理本地和云端 ncqq-manager 面板。留空时使用上方单面板配置。",
        "default": ""
    },
    "qrcode_private": {
        "title": "二维码私聊发送",
        "type": "bool",
        "description": "群聊触发二维码时，通过私聊发送给请求者（防止他人误扫）。默认开启。",
        "default": true
    },
    "notify_group": {
        "title": "通知群号",
        "type": "string",
        "description": "管理通知推送的目标群号（掉线告警、恢复通知等）。留空则不推送群通知。",
        "default": ""
    },
    "enable_group_whitelist": {
        "title": "启用响应群白名单",
        "type": "bool",
        "description": "开启后，插件只响应 response_groups 中列出的群聊；私聊不受影响。",
        "default": false
    },
    "response_groups": {
        "title": "响应群白名单",
        "type": "string",
        "description": "允许插件响应的群号，支持逗号、空格、顿号分隔。仅在启用响应群白名单后生效。",
        "default": ""
    },
    "enable_offline_notify": {
        "title": "掉线通知",
        "type": "bool",
        "description": "开启后定时检测实例状态，掉线时私聊通知归属人并推送到通知群。",
        "default": true
    },
    "health_check_interval": {
        "title": "检测间隔（分钟）",
        "type": "int",
        "description": "实例健康检测的轮询间隔，单位分钟。建议 3~10 分钟。",
        "default": 5
    }
}
